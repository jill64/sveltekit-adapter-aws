branch-protection:
  main:
    required_status_checks:
      contexts:
        - test (edge-bundled)
        - test (edge-unbundled)
        - test (lambda-mono)
        - test (lambda-s3)
        - deploy (edge-bundled)
        - deploy (edge-unbundled)
        - deploy (lambda-mono)
        - deploy (lambda-s3)
